const mongoose = require('mongoose'); const { connectDB } = require('./src/config/database'); const User = require('./src/models/User'); require('dotenv').config(); (async () => { await connectDB(); const user = await User.findById('6a4b34a6454ed0444ae720ae'); console.log('Last updated:', user.updatedAt, 'Last login:', user.lastLogin, 'FCM token:', user.fcmToken); process.exit(0); })();
